name: reloado_auth
description: Reloado Auth – Secure 2FA Authentication Manager
publish_to: 'none'
version: 1.0.2+3

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  local_auth: ^2.3.0
  local_auth_android: ^1.0.56
  flutter_secure_storage: ^9.2.2
  otp: ^3.1.4
  http: ^1.2.2
  encrypt: ^5.0.3
  crypto: ^3.0.5
  shared_preferences: ^2.3.5
  mobile_scanner: ^7.0.0
  flutter_svg: ^2.0.10+1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  generate: true
